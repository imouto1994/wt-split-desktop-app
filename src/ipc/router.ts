import { app } from "./app";
import { shell } from "./shell";
import { theme } from "./theme";
import { webtoon } from "./webtoon";
import { window } from "./window";

export const router = {
  theme,
  window,
  app,
  shell,
  webtoon,
};
